// T1021.004 - Unusual SSH Connections
// Detects Windows-to-ESXi SSH connection attempts
DeviceNetworkEvents
| where RemotePort == 22 and RemoteDeviceType == "ESXi"
| project Timestamp, DeviceName, InitiatingProcessAccountName
