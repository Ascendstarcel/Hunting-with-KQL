// T1105 - Ingress Tool Transfer
// Finds large file transfers from internet sources
DeviceNetworkEvents
| where RemoteUrl endswith ".exe" or RemoteUrl endswith ".zip"
| project Timestamp, DeviceName, RemoteUrl
